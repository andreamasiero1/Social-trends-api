# Configurazione per test in locale
# Sostituisci con il tuo DATABASE_URL di produzione da Render

# DATABASE_URL=postgresql://social_trends_user:password@localhost:5432/social_trends_db
# DATABASE_URL=postgresql://your_render_db_url_here

# Per il test, puoi usare il DATABASE_URL di produzione di Render
# Vai su Render > Database > Internal Database URL e copialo qui:
DATABASE_URL=

# Email Configuration (opzionale per i test)
SMTP_SERVER=
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@social-trends-api.com

# Se vuoi testare l'invio email, configura con Gmail:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=tua-email@gmail.com
# SMTP_PASSWORD=tua-app-password
